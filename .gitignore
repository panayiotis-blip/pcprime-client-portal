# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env
.env
.env.*
!.env.example
!.env.scripts.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sensitive / large data — keep out of git
_legacy-sqlite-backup/
clients_import.csv
.vercel/
.claude/
# Real TaxisNet sample filings — contain personal data, keep local only
supabase/xsd-tep-ext-2024/**/Samples/

# Per-task brief files (transient, kept locally)
*-instructions.md

# Client rental app (contains real tenant data — seed goes to Supabase, not git)
Greson_Property_Rentals.html
SEED_greson_rental.sql
SEED_greson_live.sql

# Client management app (real client data — data goes to Supabase, not git)
Greson_Management_Dashboard.html
greson_mgmt_live.json
greson_mgmt_clean.json
SEED_greson_mgmt.sql

# Client payroll app (real employee data — seed goes to Supabase, not git)
km-payroll-app.html
SEED_km_payroll.sql
